<UserControl
    x:Class="InvadersUWP_MVVM.View.AnimatedImage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:InvadersUWP_MVVM.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <Storyboard x:Name="invaderShotStoryboard">
            <DoubleAnimation From="1" To="0" Storyboard.TargetName="invaderShot"
                             Storyboard.TargetProperty="Opacity" Duration="0:0:1"/>
        </Storyboard>
        <Storyboard x:Name="flashStoryboard">
            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility"  Duration="0:0:1"
                                           Storyboard.TargetName="playerShip"/>
        </Storyboard>
    </UserControl.Resources>

    <Grid>
        <Image x:Name="image" Stretch="Fill"/>
    </Grid>
</UserControl>
